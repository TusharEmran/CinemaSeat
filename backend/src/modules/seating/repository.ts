/**
 * All seat_claims SQL lives here - one file, so the invariant has one blast
 * radius. If you are about to write a claim query somewhere else, do not.
 */
export {};
// TODO: insertHoldOnConflictDoNothing(), releaseByHoldId(),
//       markBooked(), sweepExpired(limit), liveClaimsForShowtime()
