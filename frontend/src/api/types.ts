/**
 * Response types. Keep these in step with backend/src/modules/*/schemas.ts and
 * with the request shapes documented in README.md — judges test against what
 * the README says, so a drift there is a real failure, not a docs nit.
 */
export {};
// TODO: SeatStatus = 'AVAILABLE' | 'HELD' | 'BOOKED'
// TODO: SeatMap, Hold, Booking, BookingStatus, ApiError
