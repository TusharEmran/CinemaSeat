/**
 * Final booking state and the ticket QR.
 *
 * Polls until terminal: CONFIRMED, PAYMENT_FAILED, EXPIRED or REFUNDED. Each
 * gets its own plain-language explanation — a user who lost their seat deserves
 * to be told why, not shown a spinner that never resolves.
 */
export {};
